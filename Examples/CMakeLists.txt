# Examples for ATL.
# Add here the directories created for each example.

# Example 1 : Loads a NullDriver, 1000 Example Resource, and a NullImage.
# Shows if the ATL library is currently working.

add_subdirectory( FirstExample )